import React from "react";

import Modal from "../modal/Modal";

const Login = () => {
  return (
    <Modal
      headerStyle={{
        backgroundColor: "white",
        width: "100%"
      }}
      bodyStyle={{
        backgroundColor: "white",
        width: "100%"
      }}
    />
  );
};

export default Login;
